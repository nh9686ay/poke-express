<div class="text-center">
    <h1>{{this.name}}</h1>
    <img src="{{this.img}}" alt="a picture of {{this.name}}">
    <div class="my-3">
        <a href="/pokemon" class="btn btn-info">Go Back</a>
        <form action="/pokemon/{{this.id}}?_method=DELETE" method="POST" class="d-inline">
            <input type="submit" value="Release :(" class="btn btn-danger">
        </form>
    </div>
</div>